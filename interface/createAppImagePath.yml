%YAML 1.2
---
INFO:
  note: 移动端App需要查看影像时, 调用该接口, 返回移动端查阅影像的URL地址
  api: http://ip:8087/siitservice/createAppImagePathApi/createImagePath

PARAMS:
  barcode: value            # 不为空, 单据条形码
  useraccount: value        # 不为空, 当前操作用户
  rescanbtn:                # 可空, 是否允许发起重扫, 1/允许, 0/不允许, 默认0
  datasource:               # 可空, 调用系统来源

...